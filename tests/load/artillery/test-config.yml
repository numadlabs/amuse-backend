config:
  target: "http://localhost:3001"
  socketio:
    transports: ["websocket"]
scenarios:
  - name: My Socket.IO test
    engine: socketio # Enable the Socket.IO engine
    flow:
      - on:
        channel: "haha"
        handler:
          - log: "Received response: {{ response }}"
        url: "http://localhost:3001"

      # - think: 2 # Wait for 2 seconds before closing the connection
      # - emit:
      #     channel: "register"
      #     data: "Hello world!"
      #     response:
      #       channel: "oz_response"
      #       data: "hello"
